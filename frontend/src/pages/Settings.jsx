function Settings() {
    return (
        <div className="animate-fade-in">
            <h1 className="text-3xl font-bold mb-6">Settings</h1>
            <div className="glass-card p-6">
                <p className="text-[var(--color-text-secondary)]">Settings page — profile, voice clone setup, and notification preferences.</p>
            </div>
        </div>
    );
}

export default Settings;
