function Send() {
    return (
        <div className="animate-fade-in">
            <h1 className="text-3xl font-bold mb-6">Send Payment</h1>
            <div className="glass-card p-6">
                <p className="text-[var(--color-text-secondary)]">Send page — will include QR scanner, recipient details form, and amount input.</p>
            </div>
        </div>
    );
}

export default Send;
